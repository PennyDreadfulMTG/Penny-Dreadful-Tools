<h1>Achievements</h1>
<section>
    <dl>
        {{#achievement_descriptions}}
        <dt class='{{class}}' title="{{detail}}">{{title}}</dt>
        <dd>{{{description_safe}}}</dd>
        {{/achievement_descriptions}}
    </dl>
</section>

{{#person_url}}
    <section>
        <p>🏆 = earned.</p>
    </section>
{{/person_url}}
