<section>
    <h2>{{name}}</h2>
    <img src="{{img_url}}" alt="{{name}}" class="card-img">
</section>
<section>
    <h2>Decks containing {{name}}</h2>
    {{> decks}}
    <p>{{#played_competitively}}Competitive record of decks with this card in is {{> record}}{{/played_competitively}}</p>
</section>
{{> legal}}
