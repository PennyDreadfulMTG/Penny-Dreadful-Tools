<h1>News Admin</h1>
<section>
    {{#news}}
        <form method="post">
            <div class="field">
                <label for="date">Date</label>
                <input type="text" name="date" value="{{form_date}}">
            </div>
            <div class="field">
                <label for="title">Title</label>
                <input type="text" name="title" value="{{title}}">
            </div>
            <div class="field">
                <label for="body">Body</label>
                <textarea name="body">{{body}}</textarea>
            </div>
            {{#id}}
                <div class="field">
                    <input type="hidden" name="id" value="{{id}}">
                </div>
                <button type="submit" name="action" value="delete">Delete</button>
            {{/id}}
            <button type="submit" name="action" value="add">{{#id}}Update{{/id}}{{^id}}Add{{/id}}</button>
        </form>
    {{/news}}
</section>
