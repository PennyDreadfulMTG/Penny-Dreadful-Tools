                </div> {{! .content }}
            </main>
            <footer>
                {{#menu}}
                    <section class="{{#admin_only}}admin{{/admin_only}} {{#demimod_only}}demimod{{/demimod_only}}">
                        <p><b><a class="menuitem" href="{{url}}">{{name}}</a></b></p>
                        {{#submenu}}
                            <p class="{{#admin_only}}admin{{/admin_only}} {{#demimod_only}}demimod{{/demimod_only}}"><a {{#is_external}}class="external"{{/is_external}} href="{{url}}">{{name}}</a></p>
                        {{/submenu}}
                    </section>
                {{/menu}}
                <section>
                    <p>Penny Dreadful tournaments and league are sponsored by <a class="external" href="https://cardhoarder.com/">Cardhoarder</a>.</p>
                    <p><a href="https://cardhoarder.com/"><img class="sponsor-logo" src="{{cardhoarder_logo_url}}" alt="" width="470" height="366"></a></p>
                </section>
                <section>
                    <p>Popup card images courtesy of <a href="https://deckbox.org/">deckbox.org</a>.</p>
                </section>
                <section>
                    {{> contact}}
                </section>
            </footer>
            <footer class="status-bar">
                <p class="loading"><span class="spinner"></span> <span class="text">Loading…</span></p>
            </footer>
        </div> {{! .layout }}
        <nav>
            {{> menu}}
        </nav>
        <div class="scrim"></div>
        {{#tooltips_url}}
            <script defer src="{{tooltips_url}}"></script>
        {{/tooltips_url}}
        {{> jsdependencies}}
        <script defer src="/static/js/tipped.min.js"></script> {{* The latest open source (pro) version of Tipped is not on any CDN. }}
        <script defer src="{{js_url}}"></script>
        <script defer src="{{bundle_url}}"></script> {{* React components use pd.js but not vice versa so load this after. }}
    </body>
</html>
