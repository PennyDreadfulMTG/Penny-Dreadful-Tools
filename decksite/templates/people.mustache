<section>
    <h2>People</h2>
    <table>
        <thead>
            <th>Name</th>
            <th data-sortInitialOrder="desc"># Decks</th>
            <th data-sortInitialOrder="desc"># Comp</th>
            <th data-sortInitialOrder="desc">Record</th>
        </thead>
        {{#people}}
            <tr>
                <td><a href="{{url}}">{{name}}</a></td>
                <td class="n"><a href="{{url}}">{{num_decks}}</a></td>
                <td class="n"><a href="{{url}}">{{num_competitions}}</a></td>
                <td>{{#show_record}}<a href="{{url}}">{{wins}}–{{losses}}</a>{{/show_record}}</td>
            </tr>
        {{/people}}
    </table>
</section>
