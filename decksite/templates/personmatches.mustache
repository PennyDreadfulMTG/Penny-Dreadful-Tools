<section>
    <h2>Matches</h2>
    <table>
        <thead>
            <tr>
                {{#show_hero}}<th>Person</th>{{/show_hero}}
                <th>Deck</th>
                <th>Opponent</th>
                <th>Opponent's Deck</th>
                <th class="n">Result</th>
                <th>Competition</th>
                <th>Date</th>
                <th>MTGO Log</th>
            </tr>
        </thead>
        <tbody>
            {{#matches}}
                <tr>
                    {{#show_hero}}<td><a href="{{person_url}}">{{person}}</a></td>{{/show_hero}}
                    <td><a href="{{deck_url}}">{{deck_name}}</a></td>
                    <td><a href="{{opponent_url}}">{{opponent}}</a></td>
                    <td>
                        {{#opponent_deck_url}}
                            <a href="{{opponent_deck_url}}">{{opponent_deck_name}}</a>
                        {{/opponent_deck_url}}
                    </td>
                    <td class="n">{{game_wins}}–{{game_losses}}</td>
                    <td><a href="{{competition_url}}">{{competition_type_name}}</a></td>
                    <td data-text="{{date_sort}}">{{display_date}}</td>
                    <td>{{#mtgo_id}}<a href="{{log_url}}">{{mtgo_id}}</a>{{/mtgo_id}}</td>
                </tr>
            {{/matches}}
        </tbody>
    </table>
</section>
