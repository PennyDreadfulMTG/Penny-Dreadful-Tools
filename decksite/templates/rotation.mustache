<section>
    <p>{{rotation_msg}}</p>
    {{#runs}}
        <p>{{runs}} ({{runs_percent}}%) of 168 runs completed. {{num_cards}} cards have hits.</p>
    {{/runs}}
</section>
{{#runs}}
    <section>
        <div
            class="rotationtable"
            data-page-size="{{page_size}}">
        </div>
        {{> rotationkey}}
    </section>
{{/runs}}
