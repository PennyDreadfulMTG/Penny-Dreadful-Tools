{
  "fix": [
    {
      "batch_id": "f-12537-1",
      "branch": "sweep/12537-anything-with-a-title-attribute-that-isn",
      "files_changed": 1,
      "head_sha": "2b5fd4b419d9176fc6fc045b91f74d6840c2e2ca",
      "issue": 12537,
      "lines_changed": 4,
      "lint": "passed",
      "notes": "The two `<th title=\"Opponent's Match Win\">OMW</th>` sortable table headers will now show help cursor instead of pointer cursor, since `[title]:not(a)` has higher CSS specificity (0,1,1) than `main th` (0,0,2). This is acceptable \u2014 the OMW header's title attribute is its primary purpose (explaining the abbreviation), and sorting is still discoverable via the \u21c5 character. All `<a title=\"...\">` links are unaffected due to `:not(a)`. Interactive elements like `.achievement-bar` that lack title attributes are also unaffected.",
      "outcome": "pushed",
      "pr_body": "## What was wrong\n\nElements with a `title` attribute (which causes browsers to show a tooltip on hover) that were not `<a>` links were showing the default text cursor (I-beam) or arrow cursor. This gave users no indication that hovering would reveal additional information. The issue specifically called out achievements on the player detail page (`<li class=\"button\" title=\"...\">` and `<span class=\"earned\" title=\"...\">`) as examples.\n\n## What changed\n\nAdded `[title]:not(a) { cursor: help; }` to `shared_web/static/css/pd.css` near the existing cursor rules section (around line 3233). This makes all non-link elements with a `title` attribute show the help cursor (\u2753), signalling to users that hovering will reveal a tooltip. Links (`<a>`) are excluded since they already show the pointer cursor and their primary action is navigation, not tooltip display.\n\n## How it was validated\n\n- `uv run --frozen python dev.py lint` \u2014 passed\n- `uv run --frozen python dev.py unit` \u2014 845 passed, 1 skipped\n- Manual review of `git diff origin/master...` confirmed only 4 lines added, all serving #12537\n\nFixes #12537",
      "pr_title": "Show help cursor on elements with title attribute that are not links (#12537)",
      "recorded_at": "2026-09-01T18:22:10Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python dev.py unit",
        "detail": "845 passed, 1 skipped",
        "result": "passed"
      }
    }
  ],
  "issue": 12537,
  "triage": [
    {
      "batch_id": "t-12539-1",
      "class": "easy-fix",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "cursor: help applies only to .active, .stars, .last-run \u2014 no general [title]:not(a) rule exists anywhere in the file",
          "ref": "shared_web/static/css/pd.css:3234",
          "type": "code"
        },
        {
          "detail": "zero results \u2014 no attribute selector on title is present in current CSS",
          "ref": "grep -n '\\[title\\]' shared_web/static/css/pd.css",
          "type": "search"
        }
      ],
      "fix_sketch": "Add `[title]:not(a) { cursor: help; }` to `shared_web/static/css/pd.css` near the existing cursor rules (around line 3234). Verify that existing higher-specificity `cursor: pointer` rules on interactive elements still win where needed.",
      "issue": 12537,
      "notes": "The change is a single CSS attribute selector rule. Risk is that any interactive non-link element that has a title attribute and a lower-specificity cursor:pointer rule could have its cursor overridden; manual spot-check of sortable table headers and similar widgets is recommended after the change.",
      "proposed_comment": null,
      "recorded_at": "2026-09-01T17:58:39Z",
      "risk": "low"
    }
  ],
  "verify": []
}
