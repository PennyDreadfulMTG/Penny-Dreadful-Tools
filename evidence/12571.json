{
  "fix": [
    {
      "batch_id": "f-12571-2",
      "branch": "sweep/12571-the-ordering-of-losing-qfs-is-not-stable",
      "files_changed": 1,
      "head_sha": "f789a8f8dfe9fcdb431a868d11693e494c6aef12",
      "issue": 12571,
      "lines_changed": 4,
      "lint": "passed",
      "notes": "Two identical one-line changes in the same file. Adding the primary key as a sort tiebreaker is a purely additive, safe change with no semantic impact on callers that supply their own `order_by`. No schema or data migration needed.",
      "outcome": "pushed",
      "pr_body": "## Problem\n\nThe competition detail page shows losing decks (those with a `finish` value) in an unstable order. When multiple decks share the same `active_date` and `finish` value (e.g. several decks that all lost in the same round), MySQL/MariaDB does not guarantee a consistent tie-breaking order, so the ordering could vary between page loads or database restarts.\n\n## Fix\n\nAppended `, d.id` to the default `ORDER BY` clause in two places in `decksite/data/deck.py`:\n\n- `load_decks_query` (line 88) \u2014 used for the lightweight deck list query\n- `load_decks_heavy` (line 159) \u2014 used for the heavy deck list query with win/loss counts\n\nBoth defaulted to `active_date DESC, d.finish IS NULL, d.finish`. Adding `d.id` as a final tiebreaker makes the sort fully deterministic since `d.id` is a unique primary key.\n\n## Validation\n\n- `uv run --frozen python dev.py lint` \u2014 passed (All checks passed)\n- `uv run --frozen python dev.py unit` \u2014 845 passed, 1 skipped, 89 deselected in 17.90s\n\nFixes #12571",
      "pr_title": "Make deck ordering deterministic by appending d.id to ORDER BY (#12571)",
      "recorded_at": "2026-09-01T18:25:06Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python dev.py unit",
        "detail": "845 passed, 1 skipped, 89 deselected in 17.90s",
        "result": "passed"
      }
    }
  ],
  "issue": 12571,
  "triage": [
    {
      "batch_id": "t-12572-1",
      "class": "easy-fix",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "default order_by is 'active_date DESC, d.finish IS NULL, d.finish' \u2014 no unique tiebreaker, so rows with identical finish are ordered arbitrarily by the DB",
          "ref": "decksite/data/deck.py:88",
          "type": "code"
        },
        {
          "detail": "competition.load_decks calls deck.load_decks(where) without specifying order_by, falling through to the non-stable default",
          "ref": "decksite/data/competition.py:100",
          "type": "code"
        },
        {
          "detail": "no commit references this issue; the non-stable sort is still present in master",
          "ref": "git log --oneline --all --grep=12571",
          "type": "search"
        }
      ],
      "fix_sketch": "In decksite/data/deck.py, append ', d.id' to the default order_by string at line 88 (and the identical default at line 159): change 'active_date DESC, d.finish IS NULL, d.finish' to 'active_date DESC, d.finish IS NULL, d.finish, d.id'. The primary key is always unique so the sort becomes fully deterministic. Two one-line changes.",
      "issue": 12571,
      "notes": "Issue explicitly notes the real answer (swiss tiebreakers) is not scraped; using d.id gives a stable but arbitrary ordering among tied finishers, which is sufficient to stop page flicker.",
      "proposed_comment": null,
      "recorded_at": "2026-09-01T17:59:02Z",
      "risk": "low"
    }
  ],
  "verify": []
}
