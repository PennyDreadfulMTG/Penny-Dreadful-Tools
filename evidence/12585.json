{
  "fix": [
    {
      "batch_id": "f-12585-2",
      "branch": "sweep/12585-the-meta-share-bar-doesn-t-show-a-true-p",
      "files_changed": 1,
      "head_sha": "069160e03eb8fa6ae525a7653a792347552669d2",
      "issue": 12585,
      "lines_changed": 4,
      "lint": "passed",
      "notes": "Pure frontend JSX fix \u2014 two arithmetic lines changed. No Python, no data, no auth touched. The only reviewer pointer: the stacked bar highlight (line 24) already used `metaShare * 100` correctly; only the label-side width was wrong.",
      "outcome": "pushed",
      "pr_body": "## What was wrong\n\n`archetype.metaShare` is a decimal fraction (e.g. `0.15` for 15%). On line 25, the remaining-width for the label div was calculated as `100 - archetype.metaShare`, which evaluated to `99.85%` instead of `85%`. This made the label div almost always overflow its container regardless of the actual meta-share value.\n\n## What changed\n\nIn `shared_web/static/js/metagamegrid.jsx`:\n\n- Changed the label div width from `` `${100 - archetype.metaShare}%` `` to `` `${(1 - archetype.metaShare) * 100}%` `` so it correctly occupies the remaining portion of the row.\n- Moved the 8 px spacing from an inline `marginLeft` on the label div to a `gap: \"8px\"` on the parent `flex-row` div, so the two children' widths can honestly sum to 100% without overflowing.\n\n## Validation\n\n`uv run --frozen python dev.py lint` \u2014 **passed**. The unit test suite requires a live MariaDB instance which was not provisioned; `metagamegrid.jsx` is a React component with no Python unit tests, so no relevant tests were skipped.\n\nFixes #12585",
      "pr_title": "Fix meta share bar width calculation (#12585)",
      "recorded_at": "2026-09-01T18:12:41Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python dev.py unit shared_web",
        "detail": "Unit test suite requires a running MariaDB instance; no DB was provisioned. The changed file (metagamegrid.jsx) is a React component with no Python unit tests \u2014 the suite failure is environmental, not caused by this change.",
        "result": "env-blocked"
      }
    }
  ],
  "issue": 12585,
  "triage": [
    {
      "batch_id": "t-12587-1",
      "class": "easy-fix",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "width: `${100 - archetype.metaShare}%` \u2014 if metaShare is a 0-1 decimal (e.g. 0.25), this yields 99.75% instead of the correct 75%, causing total flex widths to exceed 100%",
          "ref": "shared_web/static/js/metagamegrid.jsx:25",
          "type": "code"
        },
        {
          "detail": "bar width: `${archetype.metaShare * 100}%` \u2014 bar itself is correct but remainder div formula is wrong",
          "ref": "shared_web/static/js/metagamegrid.jsx:24",
          "type": "code"
        },
        {
          "detail": "meta_share computed as SUM(wins+losses+draws)/total_matches \u2014 a 0-1 fraction confirmed",
          "ref": "decksite/data/archetype.py:99",
          "type": "code"
        },
        {
          "detail": "'meta_share': float(row['meta_share']) \u2014 stored as float, confirms 0-1 range",
          "ref": "decksite/data/archetype.py:112",
          "type": "code"
        },
        {
          "detail": "marginLeft: \"8px\" on remainder div takes additional space beyond the 100% total, compounding the layout error",
          "ref": "shared_web/static/js/metagamegrid.jsx:25",
          "type": "code"
        }
      ],
      "fix_sketch": "In metagamegrid.jsx line 25, change `width: \\`${100 - archetype.metaShare}%\\`` to `width: \\`${(1 - archetype.metaShare) * 100}%\\``. To handle the 8px margin without overflow, either add `gap: '8px'` to the parent flex-row div and remove the inline marginLeft, or use `width: \\`calc(${(1 - archetype.metaShare) * 100}% - 8px)\\``. Touch only metagamegrid.jsx.",
      "issue": 12585,
      "notes": "The chart.js comment in the issue is a musing from the owner but fixing the math and margin is a valid, self-contained fix. The chart.js path would be a separate larger effort.",
      "proposed_comment": null,
      "recorded_at": "2026-09-01T17:58:50Z",
      "risk": "low"
    }
  ],
  "verify": []
}
