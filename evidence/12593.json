{
  "fix": [
    {
      "batch_id": "f-12593-1",
      "branch": "sweep/12593-find-a-solution-for-the-badge-of-archety",
      "files_changed": 2,
      "head_sha": "876efa11b24ac788d8d4644a27b97d5a50cccb61",
      "issue": 12593,
      "lines_changed": 38,
      "lint": "passed",
      "notes": "The `ul.menu > li { position: relative }` rule changes the containing block for `.submenu { position: absolute; top: 0 }` from `nav` to `<li>` at medium/narrow widths. Submenus now align to the hovered item's top rather than nav's top, leaving a small gap above the submenu panel equal to the item's distance from the nav top. This is a minor visual trade-off; the badge clickability fix is the primary goal. No items are known to have both a badge and a submenu simultaneously today (the Metagame badge is only revealed to mods via JS), so the practical impact is minimal.",
      "outcome": "pushed",
      "pr_body": "## What was wrong\n\nThe archetype-sort badge in the nav menu relied on a `z-index: 3` hack on `.badge-holder` to paint above the `<a class=\"item\">` link and remain clickable. The badge appeared *before* the item in the HTML, so without the elevated z-index the item link (which came later in the DOM) would paint over it. The z-index value (3) conflicted with the intentional z-index design table in `pd.css`, where level 3 was explicitly reserved as empty.\n\n## What changed\n\n**`decksite/templates/menu.mustache`** \u2014 Moved the `{{#badge}}\u2026{{/badge}}` block to *after* the `<a class=\"item\">` element. The badge now appears later in the DOM, so it naturally paints above the item link in the same stacking context \u2014 no z-index needed.\n\n**`shared_web/static/css/pd.css`**:\n- Replaced the old `.badge-holder { position: relative }` rule with `ul.menu > li { position: relative }`, making the `<li>` the containing block for the absolutely-positioned `.badge`. The badge's `top: 13px; right: 1.5rem` values are unchanged and remain visually correct (the badge-holder had zero height and sat at `<li>`'s top anyway, so the coordinate origin is the same).\n- Removed the `z-index: 3` hack block on `.badge-holder` (and its explanatory comment).\n- Updated all six `> .item + .submenu` / `> .current + .submenu` adjacent-sibling selectors to general-sibling `~` selectors, since the badge-holder `<div>` can now appear between `.item` and `.submenu` in the DOM.\n\n## How it was validated\n\n- `uv run --frozen python dev.py lint` \u2014 passed\n- `uv run --frozen python -m pytest decksite/ -x -m 'not functional and not perf'` \u2014 426 passed, 6 deselected\n- The existing `test_menu_badge_is_entirely_linked` test continues to pass; it verifies the badge link renders correctly in the template.\n\nFixes #12593",
      "pr_title": "Fix badge z-index hack using DOM order and li as containing block (#12593)",
      "recorded_at": "2026-09-01T18:33:49Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python -m pytest decksite/ -x -m 'not functional and not perf'",
        "detail": "426 passed, 6 deselected in 8.50s",
        "result": "passed"
      }
    }
  ],
  "issue": 12593,
  "triage": [
    {
      "batch_id": "t-12598-1",
      "class": "easy-fix",
      "confidence": "medium",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "Comment explicitly labels z-index:3 on .badge-holder a hack; the badge appears before <a class=item> in the DOM so it needs elevation to be clickable",
          "ref": "shared_web/static/css/pd.css:1613-1619",
          "type": "code"
        },
        {
          "detail": "{{#badge}}<div class=badge-holder>...</div>{{/badge}} appears before <a class=item> in the li, causing the z-index stacking problem",
          "ref": "decksite/templates/menu.mustache:5-13",
          "type": "code"
        },
        {
          "detail": ".badge-holder has position:relative so .badge (position:absolute) anchors to it; z-index:3 lifts it above the following item link",
          "ref": "shared_web/static/css/pd.css:2665-2670",
          "type": "code"
        }
      ],
      "fix_sketch": "In decksite/templates/menu.mustache, move the {{#badge}}\u2026{{/badge}} block to after the <a class=\"item\"> element. In shared_web/static/css/pd.css, add position:relative to the <li> (or the enclosing element), remove position:relative from .badge-holder, and remove the z-index:3 hack \u2014 the badge will then position absolutely relative to the li and paint above the item link naturally by DOM order.",
      "issue": 12593,
      "notes": "Confidence medium because the badge positioning interacts with the dropdown submenu logic (css comment on pd.css:2664 warns menu items need static positioning for dropdowns); visual regression testing is advisable before merging.",
      "proposed_comment": null,
      "recorded_at": "2026-09-01T17:58:17Z",
      "risk": "low"
    }
  ],
  "verify": []
}
