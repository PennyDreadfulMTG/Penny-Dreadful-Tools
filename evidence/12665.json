{
  "fix": [
    {
      "batch_id": "f-12665-1",
      "branch": "sweep/12665-provide-a-way-to-dismiss-the-popover-men",
      "files_changed": 3,
      "head_sha": "7ab51f786c3530cdf4c590acd1a8b8fee837d345",
      "issue": 12665,
      "lines_changed": 42,
      "lint": "passed",
      "notes": "The scrim click handler previously used classList.toggle rather than classList.remove; the new PD.closeMenu() always removes the class, which is more correct (clicking the scrim should always close, never re-open). The Escape listener is attached to document every time PD.initMenu is called \u2014 safe since the page is single-loaded, but worth noting if initMenu is ever called more than once.",
      "outcome": "pushed",
      "pr_body": "## What was wrong\n\nThe mobile popover nav menu could only be dismissed by clicking the scrim overlay or selecting a menu item \u2014 there was no keyboard shortcut and no explicit close button, making it awkward especially for keyboard users.\n\n## What changed\n\n- **`shared_web/static/js/pd.js`**: Extracted a `PD.closeMenu()` helper (removes `.showing` from all `.scrim` and `nav` elements). Wired it to the scrim click handler (replacing the toggle with a deterministic remove), to any `.close-menu` button click, and to a `document` `keydown` listener that fires on `Escape`.\n- **`decksite/templates/menu.mustache`**: Added a `<button class=\"close-menu\" aria-label=\"Close menu\">\u2715</button>` at the top of the nav template.\n- **`shared_web/static/css/pd.css`**: Styled `.close-menu` as a hidden absolute-positioned button that becomes visible (`display: block`) only when `nav.showing` is active, placing the \u2715 in the top-right corner of the open drawer.\n\n## Validation\n\n- `uv run --frozen python dev.py lint` \u2014 passed (ruff: All checks passed)\n- `uv run --frozen python dev.py unit` \u2014 687 passed, 1 skipped (unrelated functional test)\n- JS/CSS changes are not covered by the Python unit suite; manual inspection of the diff confirms correctness.\n\nFixes #12665",
      "pr_title": "Add Escape key and close button to dismiss the popover menu (#12665)",
      "recorded_at": "2026-08-23T21:52:43Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python dev.py unit",
        "detail": "687 passed, 1 skipped in 18.43s",
        "result": "passed"
      }
    }
  ],
  "issue": 12665,
  "triage": [
    {
      "batch_id": "t-12670-1",
      "class": "easy-fix",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "PD.initMenu registers only hamburger-click and scrim-click handlers; no keydown/keyup listener for Escape exists anywhere in the file",
          "ref": "shared_web/static/js/pd.js:43-61",
          "type": "code"
        },
        {
          "detail": "The nav menu template has no close/X button element",
          "ref": "decksite/templates/menu.mustache:1-31",
          "type": "code"
        },
        {
          "detail": "No results \u2014 Escape key dismissal is entirely absent",
          "ref": "grep -n 'Escape|keydown|ESC' shared_web/static/js/pd.js",
          "type": "search"
        }
      ],
      "fix_sketch": "In shared_web/static/js/pd.js: add a document keydown listener that calls the same close logic as the scrim click handler when e.key === 'Escape' (~5 lines, inside PD.initMenu). Optionally add a close/X button to decksite/templates/menu.mustache (1 line) and style it in pd.css (~10 lines). Total ~20-30 lines across 2-3 files.",
      "issue": 12665,
      "notes": "The issue also shows a screenshot requesting an explicit dismiss button with an X icon; implementing Esc alone is a minimum fix. Adding the button is a small additive change.",
      "proposed_comment": null,
      "recorded_at": "2026-08-23T21:36:27Z",
      "risk": "low"
    }
  ],
  "verify": []
}
