{
  "fix": [
    {
      "batch_id": "f-12715-1",
      "branch": "sweep/12715-make-the-archetypes-to-assign-badge-into",
      "files_changed": 1,
      "head_sha": "8ad7e65879ecd87be1419f8fe5ae8eb12c8ed95a",
      "issue": 12715,
      "lines_changed": 3,
      "lint": "passed",
      "notes": "The `border-radius: 50%` produces a perfect circle only when the element is square. For counts \u2265 10 the `min-width` will expand beyond `height`, resulting in a pill shape rather than a circle \u2014 this is standard notification badge behavior and visually correct. `var(--white)` was deliberately avoided for `color` because it maps to near-black in dark mode.",
      "outcome": "pushed",
      "pr_body": "## What was wrong\n\nThe `.badge` span used to display the archetypes-to-assign count in the site nav had no background, so the number floated over menu items with no visual container \u2014 it didn't read as a notification badge.\n\n## What changed\n\nThree CSS properties were added to `.badge` in `shared_web/static/css/pd.css`:\n\n- `background-color: var(--red)` \u2014 uses the site's existing red variable (which adapts for dark mode).\n- `border-radius: 50%` \u2014 turns the already-square element (`height: 1.333em`, `min-width: 1.333em`) into a true circle.\n- `color: white` \u2014 hardcoded white so text is always legible over the red background in both light and dark mode (using `var(--white)` would invert to near-black in dark mode).\n\nNo template or Python changes were needed.\n\n## Validation\n\n`uv run --frozen python dev.py lint` exits 0. No unit tests cover pure CSS; the badge rendering can be verified visually by loading the decksite with an admin account that has archetypes to assign.\n\nFixes #12715",
      "pr_title": "Make archetypes-to-assign badge a real circle badge (#12715)",
      "recorded_at": "2026-08-23T21:45:17Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python dev.py lint",
        "detail": "Exit code 0 \u2014 no CSS linting step in this repo's lint suite; ruff/mypy pass cleanly on Python files. Unit tests not run (database unavailable in cloud env; CSS change has no Python-side tests).",
        "result": "passed"
      }
    }
  ],
  "issue": 12715,
  "triage": [
    {
      "batch_id": "t-12715-1",
      "class": "easy-fix",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "The .badge class has correct sizing (height: 1.333em, min-width: 1.333em) but no border-radius, no background-color, and no color \u2014 so it renders as plain text with no circular pill shape",
          "ref": "shared_web/static/css/pd.css:2575",
          "type": "code"
        },
        {
          "detail": "Badge renders as <span class=\"admin demimod badge edit_archetypes\"> \u2014 a bare span with no visual circle behind it",
          "ref": "decksite/templates/menu.mustache:5",
          "type": "code"
        },
        {
          "detail": "Most recent badge CSS work only added hover treatment, didn't add circular background",
          "ref": "64f8ce39",
          "type": "commit"
        }
      ],
      "fix_sketch": "Add `border-radius: 50%`, a `background-color` (e.g. var(--accent-color) or a red/orange notification color), and `color: white` to `.badge` in `shared_web/static/css/pd.css:2575`. Also verify the sizing constants (height and min-width equal) produce a true circle. No template or Python changes needed.",
      "issue": 12715,
      "notes": "The badge already has the correct equal height/min-width sizing \u2014 purely a CSS addition to make it look like a notification dot rather than plain text.",
      "proposed_comment": null,
      "recorded_at": "2026-08-23T21:34:35Z",
      "risk": "low"
    }
  ],
  "verify": []
}
