{
  "fix": [
    {
      "batch_id": "f-12717-1",
      "branch": "sweep/12717-player-names-should-be-links-on-admin-pe",
      "files_changed": 2,
      "head_sha": "ff9a4c50f99c7d31d6d944c8b803bc5833b9fcd3",
      "issue": 12717,
      "lines_changed": 6,
      "lint": "passed",
      "notes": "Straightforward two-file change: URL generation in the view __init__ and anchor tags in the template. The `person` route accepts `person_id` as an int parameter, which matches the `subject_id`/`creator_id` fields from the `person_note` table.",
      "outcome": "pushed",
      "pr_body": "## What was wrong\n\nOn `/admin/people/notes`, the Subject and Creator columns showed player names as plain text with no hyperlinks, making it hard to navigate to a player's profile from the notes admin page.\n\n## What changed\n\n- `decksite/views/player_notes.py`: Added `from flask import url_for` and, in the `__init__` loop, set `n.subject_url = url_for('person', person_id=n.subject_id)` and `n.creator_url = url_for('person', person_id=n.creator_id)` on each note.\n- `decksite/templates/playernotes.mustache`: Wrapped the `{{subject}}` and `{{creator}}` cells in `<a href=\"{{subject_url}}\">` and `<a href=\"{{creator_url}}\">` respectively.\n\n## Validation\n\n- `uv run --frozen python dev.py lint` \u2014 passed (All checks passed)\n- `uv run --frozen python dev.py unit` \u2014 687 passed, 1 skipped, 89 deselected\n\nFixes #12717",
      "pr_title": "Make player names links on /admin/people/notes (#12717)",
      "recorded_at": "2026-08-23T21:41:42Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python dev.py unit",
        "detail": "687 passed, 1 skipped, 89 deselected in 18.60s",
        "result": "passed"
      }
    }
  ],
  "issue": 12717,
  "triage": [
    {
      "batch_id": "t-12722-1",
      "class": "easy-fix",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "Subject is rendered as `<td class=\"person\">{{subject}}</td>` \u2014 plain text, no anchor tag",
          "ref": "decksite/templates/playernotes.mustache:14",
          "type": "code"
        },
        {
          "detail": "Creator is rendered as `<td class=\"person\">{{creator}}</td>` \u2014 plain text, no anchor tag",
          "ref": "decksite/templates/playernotes.mustache:15",
          "type": "code"
        },
        {
          "detail": "load_notes() already returns subject_id and creator_id alongside the display name strings, so the IDs needed to build URLs are available",
          "ref": "decksite/data/person.py:load_notes",
          "type": "code"
        },
        {
          "detail": "Route /people/id/<int:person_id>/ exists for linking to a person by ID (endpoint name 'person')",
          "ref": "decksite/controllers/people.py:23",
          "type": "code"
        }
      ],
      "fix_sketch": "In decksite/views/player_notes.py, add URL generation in __init__ by iterating notes and setting n.subject_url = url_for('person', person_id=n.subject_id) and n.creator_url = url_for('person', person_id=n.creator_id). Then update decksite/templates/playernotes.mustache to wrap subject/creator with <a href=\"{{subject_url}}\"> and <a href=\"{{creator_url}}\"> respectively. ~10 lines total.",
      "issue": 12717,
      "notes": "The subject_id and creator_id columns are already returned by the query, so no SQL change is needed. Use url_for('person', person_id=...) matching the existing pattern in people.py.",
      "proposed_comment": null,
      "recorded_at": "2026-08-23T21:36:27Z",
      "risk": "low"
    }
  ],
  "verify": []
}
