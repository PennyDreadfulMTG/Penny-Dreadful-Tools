{
  "fix": [
    {
      "batch_id": "f-12781-1",
      "branch": "sweep/12781-stop-dropping-old-data-for-prices-now-th",
      "files_changed": 1,
      "head_sha": "4b2e74ea77f852e94bc249e29cef6f344209e8ed",
      "issue": 12781,
      "lines_changed": 6,
      "lint": "passed",
      "notes": "The cleanup() function remains in the file as dormant code per the issue comment suggesting a future archival approach may be needed for query performance. The store() function previously returned count*20 (a rough row count); that return value was only consumed by the now-removed cleanup call, so the signature is safely tightened to None.",
      "outcome": "pushed",
      "pr_body": "## What was wrong\n\n`fetch()` in `price_grabber/price_grabber.py` called `cleanup(count)` after every price fetch, which deleted all `low_price` rows older than one month (or last rotation, whichever was more recent). Now that there is enough disk space, this data loss is unnecessary.\n\n## What changed\n\n- Removed the `cleanup(count)` call from `fetch()`. The `cleanup()` function itself is left in place (dormant) so it can be repurposed for a future archival strategy if query performance requires it.\n- Cleaned up the now-dead return value from `store()` (changed return type from `int` to `None`, removed `return count * 20`).\n\n## How it was validated\n\n- `uv run --frozen python dev.py lint` \u2014 passed (all checks passed).\n- `uv run --frozen python dev.py unit` \u2014 687 passed, 1 skipped, 89 deselected.\n\nFixes #12781",
      "pr_title": "Stop dropping old price data by removing cleanup() call (#12781)",
      "recorded_at": "2026-08-23T21:44:11Z",
      "risk": "low",
      "tests": {
        "command": "uv run --frozen python dev.py unit",
        "detail": "687 passed, 1 skipped, 89 deselected in 18.65s",
        "result": "passed"
      }
    }
  ],
  "issue": 12781,
  "triage": [
    {
      "batch_id": "t-12781-1",
      "class": "easy-fix",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "cleanup() deletes rows from low_price older than start of season or 1 month, whichever is sooner \u2014 exactly the 'dropping old data' the issue describes",
          "ref": "price_grabber/price_grabber.py:63-71",
          "type": "code"
        },
        {
          "detail": "cleanup(count) is called unconditionally every time prices are fetched \u2014 still present in current master",
          "ref": "price_grabber/price_grabber.py:34",
          "type": "code"
        }
      ],
      "fix_sketch": "Remove the `cleanup(count)` call at price_grabber/price_grabber.py:34 from `fetch()`. The `cleanup()` function itself can stay dormant or be deleted. Note: issue comment warns about eventual archiving need for performance; deleting the call is the minimum change to stop data loss.",
      "issue": 12781,
      "notes": "Risk is disk growth \u2014 low_price will accumulate all historic prices indefinitely. The issue author's own comment acknowledges this; an archiving strategy may be needed separately.",
      "proposed_comment": null,
      "recorded_at": "2026-08-23T21:37:49Z",
      "risk": "medium"
    }
  ],
  "verify": []
}
