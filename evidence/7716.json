{
  "fix": [],
  "issue": 7716,
  "triage": [
    {
      "batch_id": "t-7729-1",
      "class": "already-fixed",
      "confidence": "high",
      "duplicate_of": null,
      "evidence": [
        {
          "detail": "Commit 'Get total results when fetching slice of results for cards' removed load_cards_count and added COUNT(*) OVER () AS total to load_cards, making a single query return both data and total count",
          "ref": "b0c0b82e",
          "type": "commit"
        },
        {
          "detail": "Current load_cards SQL contains COUNT(*) OVER () AS total; function returns tuple (cs, rs[0]['total']) \u2014 same pattern as the post-fix load_decks",
          "ref": "decksite/data/card.py:303",
          "type": "code"
        },
        {
          "detail": "Returns no results; load_cards_count no longer exists in the codebase",
          "ref": "grep -rn 'load_cards_count' decksite/",
          "type": "search"
        }
      ],
      "fix_sketch": null,
      "issue": 7716,
      "notes": "The commit references #12412 rather than #7716, but the code change is exactly what this issue described. Safe to close.",
      "proposed_comment": "This was resolved in commit b0c0b82e (May 2024), which removed `load_cards_count` and added `COUNT(*) OVER () AS total` directly to the `load_cards` SQL, matching the approach already used by `load_decks`. No separate count query exists any longer.",
      "recorded_at": "2026-09-01T23:00:33Z",
      "risk": "low"
    }
  ],
  "verify": [
    {
      "batch_id": "v-8285-1",
      "final_comment": "Resolved in commit b0c0b82e ('Get total results when fetching slice of results for cards'), which removed load_cards_count entirely and embedded COUNT(*) OVER () AS total directly in the load_cards SQL (decksite/data/card.py:303), so a single query now returns both the result set and the total count \u2014 matching the approach used by load_decks.",
      "issue": 7716,
      "own_evidence": [
        {
          "detail": "COUNT(*) OVER () AS total is embedded directly in the load_cards SQL query; load_cards returns a (cards, total) tuple from a single query with no separate count query",
          "ref": "decksite/data/card.py:303",
          "type": "code"
        },
        {
          "detail": "Zero results \u2014 load_cards_count has been entirely removed from the codebase",
          "ref": "grep -rn 'load_cards_count' decksite/",
          "type": "search"
        }
      ],
      "recent_human_activity": false,
      "recorded_at": "2026-09-01T23:06:39Z",
      "refutation_reason": null,
      "verdict": "CONFIRMED"
    }
  ]
}
